# 모든 텍스트 파일에 대해 줄 바꿈 문자를 자동으로 정규화
* text=auto eol=lf

# 쉘 스크립트 파일은 항상 LF(Unix 스타일)로 체크아웃
*.sh text eol=lf
*.bash text eol=lf

# Windows 배치 파일은 항상 CRLF로 체크아웃
*.bat text eol=crlf
*.cmd text eol=crlf

# Python 파일은 항상 LF로 체크아웃
*.py text eol=lf
*.pyw text eol=lf

# Docker 관련 파일은 항상 LF로 체크아웃
Dockerfile text eol=lf
.dockerignore text eol=lf

# 설정 파일은 항상 LF로 체크아웃
*.json text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.toml text eol=lf
*.ini text eol=lf
.env* text eol=lf

# 이진 파일은 줄 바꿈 변환을 하지 않음
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.tar.gz binary
*.woff binary
*.woff2 binary
